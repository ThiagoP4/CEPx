import { TelemetriaInterceptor } from './telemetria.interceptor';

describe('TelemetriaInterceptor', () => {
  it('should be defined', () => {
    expect(new TelemetriaInterceptor()).toBeDefined();
  });
});
